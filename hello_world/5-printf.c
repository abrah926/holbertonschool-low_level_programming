#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints a message using puts function.
 *              Escapes quotes within the string.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    printf("\"with proper grammar, but the outcome is a piece of art,");
    return (0);
}

